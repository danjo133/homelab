# ArgoCD — kss cluster overrides
server:
  ingress:
    hostname: argocd.kss.example.com

configs:
  cm:
    url: "https://argocd.kss.example.com"
    oidc.config: |
      name: Keycloak
      issuer: https://auth.kss.example.com/realms/broker
      clientID: argocd
      clientSecret: $argocd-oidc-secret:client-secret
      requestedScopes:
        - openid
        - profile
        - email
        - groups
