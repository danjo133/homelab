# OAuth2-Proxy — kss cluster overrides
extraArgs:
  oidc-issuer-url: "https://auth.kss.example.com/realms/broker"
  cookie-domain: ".kss.example.com"
  whitelist-domain: ".kss.example.com"
  redirect-url: "https://oauth2-proxy.kss.example.com/oauth2/callback"

ingress:
  hosts:
    - oauth2-proxy.kss.example.com
  tls:
    - secretName: oauth2-proxy-tls
      hosts:
        - oauth2-proxy.kss.example.com
