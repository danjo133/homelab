# ArgoCD — kcs cluster overrides
server:
  ingress:
    hostname: argocd.kcs.example.com
    enabled: false  # Uses Gateway API HTTPRoute

configs:
  cm:
    url: "https://argocd.kcs.example.com"
    oidc.config: |
      name: Keycloak
      issuer: https://auth.kcs.example.com/realms/broker
      clientID: argocd
      clientSecret: $argocd-oidc-secret:client-secret
      requestedScopes:
        - openid
        - profile
        - email
        - groups
