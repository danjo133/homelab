# CloudNativePG operator — Kubernetes-native PostgreSQL
resources:
  requests:
    cpu: 50m
    memory: 128Mi
  limits:
    memory: 256Mi

# Relaxed leader election — tolerate API server slowness on overcommitted hosts
additionalArgs:
  - --leader-lease-duration=60
  - --leader-renew-deadline=40
