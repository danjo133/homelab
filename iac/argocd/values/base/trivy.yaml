# Trivy operator — container vulnerability + compliance scanning
image:
  registry: harbor.example.com
  repository: docker.io/aquasec/trivy-operator

operator:
  scanJobsConcurrentLimit: 3
  vulnerabilityScannerEnabled: true
  sbomGenerationEnabled: true
  configAuditScannerEnabled: true
  rbacAssessmentScannerEnabled: true
  infraAssessmentScannerEnabled: true
  clusterComplianceEnabled: true

trivy:
  severity: CRITICAL,HIGH
  ignoreUnfixed: true
  # Route through Harbor proxy cache instead of mirror.gcr.io
  # (mirror.gcr.io is unreachable from cluster pods)
  image:
    registry: harbor.example.com
    repository: docker.io/aquasec/trivy
  dbRegistry: harbor.example.com
  dbRepository: docker.io/aquasec/trivy-db
  javaDbRegistry: harbor.example.com
  javaDbRepository: docker.io/aquasec/trivy-java-db
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      memory: 512Mi

policiesBundle:
  registry: harbor.example.com
  repository: docker.io/aquasec/trivy-checks

nodeCollector:
  registry: harbor.example.com
  repository: ghcr.io/aquasecurity/node-collector

excludeNamespaces: "kube-system"

serviceMonitor:
  enabled: true

resources:
  requests:
    cpu: 20m
    memory: 128Mi
  limits:
    memory: 512Mi
