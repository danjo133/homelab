apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../kustomize/base/headlamp

patches:
  - target:
      kind: ExternalSecret
      name: headlamp-oidc
    patch: |
      - op: replace
        path: /spec/target/template/data/OIDC_ISSUER_URL
        value: "https://auth.simple-k8s.example.com/realms/broker"
