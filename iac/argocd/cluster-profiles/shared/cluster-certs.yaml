apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-certs
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  project: bootstrap
  source:
    repoURL: __REPO_URL__
    targetRevision: __TARGET_REVISION__
    path: iac/argocd/clusters/__CLUSTER_NAME__/kustomize/cert-manager
  destination:
    server: https://kubernetes.default.svc
    namespace: cert-manager
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
