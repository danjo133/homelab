# SOPS-encrypted secrets template
# This file should be encrypted with sops before committing real values.
#
# To create your own encrypted secrets:
# 1. Generate an age keypair: age-keygen -o ~/.vagrant.d/sops_age_keys.txt
# 2. Update .sops.yaml with your public key
# 3. Run: sops iac/provision/nix/supporting-systems/secrets/secrets.yaml
# 4. Add your secrets in the editor that opens

cloudflare_api_token: REPLACE_WITH_SOPS_ENCRYPTED_VALUE
keycloak_admin_password: REPLACE_WITH_SOPS_ENCRYPTED_VALUE
github_token: REPLACE_WITH_SOPS_ENCRYPTED_VALUE
