apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Open WebUI — CNPG database + ExternalSecrets for DB and OIDC credentials

resources:
  - db-secret.yaml
  - db-cluster.yaml
  - oidc-secret.yaml
