# Long-lived service account token for Vault to use for TokenReview API calls
# This allows Vault (running outside the cluster) to validate Kubernetes JWTs
apiVersion: v1
kind: Secret
metadata:
  name: vault-auth-token
  namespace: vault-auth
  annotations:
    kubernetes.io/service-account.name: vault-auth
type: kubernetes.io/service-account-token
