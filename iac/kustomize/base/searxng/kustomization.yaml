apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# SearXNG — privacy-respecting metasearch engine for Open WebUI web search

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
