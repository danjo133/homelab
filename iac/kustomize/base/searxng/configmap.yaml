apiVersion: v1
kind: ConfigMap
metadata:
  name: searxng-config
  namespace: open-webui
data:
  settings.yml: |
    use_default_settings: true
    server:
      secret_key: "searxng-internal-only"
      limiter: false
      image_proxy: false
    search:
      formats:
        - html
        - json
