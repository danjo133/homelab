apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Teleport kube-agent join token ExternalSecret
# Syncs join token from Vault to K8s secret for the Helm chart

resources:
  - external-secret.yaml
