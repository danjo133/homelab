apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Open Terminal — execution environment for Open WebUI AI agents

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - secret.yaml
