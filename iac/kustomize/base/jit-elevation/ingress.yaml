apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jit-elevation
  namespace: identity
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    portal.example.com/name: "JIT Elevation"
    portal.example.com/description: "Temporary privilege escalation requests"
    portal.example.com/icon: "\U0001F6E1\uFE0F"
    portal.example.com/category: "Identity"
    portal.example.com/order: "30"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - jit.simple-k8s.example.com
      secretName: jit-elevation-tls
  rules:
    - host: jit.simple-k8s.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jit-elevation
                port:
                  number: 80
