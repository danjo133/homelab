apiVersion: v1
kind: Service
metadata:
  name: openclaw
  namespace: openclaw
spec:
  selector:
    app.kubernetes.io/name: openclaw
  ports:
    - name: http
      port: 18789
      targetPort: http
      protocol: TCP
