apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OpenClaw — multi-channel AI gateway with Signal integration

resources:
  - deployment.yaml
  - service.yaml
  - pvc.yaml
  - configmap.yaml
  - external-secret.yaml
