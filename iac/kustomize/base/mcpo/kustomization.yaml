apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# MCPO — MCP-to-OpenAPI proxy for Open WebUI tool integration

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
