apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Harbor imagePullSecret ExternalSecrets
# Deploys docker-registry secrets per namespace from Vault secret/harbor/admin

resources:
  - harbor-pull-secret.yaml
