# CiliumBGPPeeringPolicy - Generated from config.yaml
# Do not edit directly - run: make generate-network-config
#
# Configuration:
# - Router (UniFi): 10.69.50.1 with ASN 64512
# - Cilium nodes: ASN 64513
# - Advertises: LoadBalancer service IPs from CiliumLoadBalancerIPPool
#
# ASN Selection:
# Private ASN range: 64512-65534 (like 10.x.x.x for IPs - free for internal use)

apiVersion: cilium.io/v2alpha1
kind: CiliumBGPPeeringPolicy
metadata:
  name: bgp-peering-policy
spec:
  nodeSelector:
    matchLabels:
      kubernetes.io/os: linux

  virtualRouters:
    - localASN: 64513
      exportPodCIDR: false
      neighbors:
        - peerAddress: "10.69.50.1/32"
          peerASN: 64512
      serviceSelector:
        matchExpressions:
          - key: somekey
            operator: NotIn
            values:
              - never-match-this
