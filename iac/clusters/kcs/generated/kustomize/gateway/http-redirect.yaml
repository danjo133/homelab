# Auto-generated from cluster.yaml — do not edit
# Cluster: kcs — HTTP to HTTPS redirect
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: http-to-https-redirect
  namespace: istio-ingress
spec:
  parentRefs:
    - name: main-gateway
      namespace: istio-ingress
      sectionName: http
  hostnames:
    - "*.kcs.example.com"
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
