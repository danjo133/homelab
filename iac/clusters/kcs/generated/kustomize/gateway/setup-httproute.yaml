# Auto-generated from cluster.yaml — do not edit
# Cluster: kcs — Cluster Setup HTTPRoute for Gateway API ingress
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: cluster-setup
  namespace: identity
spec:
  parentRefs:
    - name: main-gateway
      namespace: istio-ingress
  hostnames:
    - "setup.mesh-k8s.example.com"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: cluster-setup
          port: 80
