# Network Configuration — Router-side settings
# Used to generate UniFi FRR BGP config
#
# BGP peers are derived automatically from iac/clusters/*/cluster.yaml
# (only clusters with cni: cilium are included)
#
# Run: make generate-network-config
# Or:  ./iac/network/generate.sh

# VLAN and subnet
vlan: 50
subnet: "10.69.50.0/24"
gateway: "10.69.50.1"

# BGP Configuration — router side
bgp:
  router:
    asn: 64512
    id: "10.69.50.1"
  password: "jklh2356ty8s3lkjzew2wstdlqk3j"
